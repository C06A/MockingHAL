---
name: build
description: Full project build — assembles the distributable archive and syntax-checks all example scripts
---

# Build

Run the full project build: assemble the distributable archive and verify all example scripts.

```bash
./gradlew build
```
